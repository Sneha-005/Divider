/**
 * Example Repository Interface
 * Define all data operations here (abstract layer)
 * Implementation goes in data/repositories/
 */

export interface IExampleRepository {
  // Define your data operations as abstract methods
  // Example:
  // getData(): Promise<Example[]>;
  // saveData(data: Example): Promise<void>;
}
